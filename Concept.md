# Aside Bookmark Sync

## 제품

Aside User 0과 Microsoft Edge User 0 사이에서 북마크를 로컬 전용으로 양방향 동기화하는 Chrome Manifest V3 확장 프로그램이다. 두 브라우저에 같은 확장을 설치하고 같은 로컬 암호를 입력하면, 각 확장은 `127.0.0.1`에만 바인딩된 동반 프로세스를 통해 북마크 변경을 교환한다.

## 기술 스택

- Chrome Manifest V3 확장 프로그램
- `chrome.bookmarks`, `chrome.storage`, `chrome.alarms`
- TypeScript와 Bun
- `127.0.0.1` 고정 포트의 짧은 HTTP 요청
- Web Crypto 호환 암호 파생 및 요청 인증

## 핵심 원칙

- 외부 서버, 클라우드 저장소, 브라우저 계정 동기화를 사용하지 않는다.
- 동반 프로세스는 루프백 인터페이스에만 바인딩한다.
- 서비스 워커의 수명에 의존하지 않고 모든 상태를 로컬 저장소에 둔다.
- 실제 사용자 프로필은 수정하지 않고, 전체 사용자 데이터 디렉터리를 복제한 폐기 가능한 프로필에서 E2E를 수행한다.
- 첫 버전의 충돌 정책은 변경 시각 기반 last-write-wins와 삭제 tombstone이다.

## 완료 조건

복제된 Aside와 Edge 프로필에서 같은 암호로 페어링한 뒤 북마크 추가, 이름/URL 수정, 삭제가 반대편에 반영되고, 다른 암호는 거부되며, 루프백 전용 통신과 원본 프로필 무변경이 증거로 남아야 한다.
