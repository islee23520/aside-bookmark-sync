# Design

## 사용자 흐름

1. 로컬 동반 프로세스를 실행한다.
2. Aside와 Edge에 압축 해제된 확장을 설치한다.
3. 각 브라우저의 확장 팝업에서 동일한 로컬 암호와 브라우저 이름을 저장한다.
4. 팝업은 연결 상태, 마지막 동기화 시각, 최근 오류를 표시한다.
5. 이후 북마크 변경은 백그라운드에서 자동 반영된다.

## 구성

### MV3 확장

- 최상위에서 북마크 이벤트 리스너를 등록한다.
- 변경 시 전체 관리 대상 북마크를 정규화해 동반 프로세스에 전송한다.
- `chrome.storage.local`에 설정, 로컬 변경 시각, tombstone, 마지막 서버 revision을 저장한다.
- `chrome.alarms`로 중단된 동기화를 재시도한다.
- 팝업은 암호 설정, 수동 동기화, 상태 확인만 제공한다.

### 로컬 동반 프로세스

- 고정 포트에서 `127.0.0.1`만 수신한다.
- 암호에서 파생한 식별자와 요청 인증값이 일치하는 클라이언트만 같은 sync room에 넣는다.
- 브라우저별 최신 snapshot을 로컬 파일에 저장하고 결정적 병합 결과와 revision을 반환한다.
- 외부 네트워크 요청을 만들지 않는다.

## 북마크 모델

- 동기화 범위는 각 브라우저의 사용자 편집 가능 북마크 루트 아래 항목이다.
- 폴더 경로와 URL 또는 폴더 제목을 이용해 안정적인 논리 키를 만든다.
- 각 항목은 `updatedAt`, `deletedAt`, `sourceClientId`를 가진다.
- 최신 시각이 이기며, 같은 시각은 `sourceClientId`의 사전순으로 결정한다.
- 삭제는 tombstone으로 전파하고 제한 기간 뒤 정리한다.

## 보안 경계

- 확장 권한은 `bookmarks`, `storage`, `alarms`와 정확한 루프백 origin만 사용한다.
- 원문 암호는 저장하거나 전송하지 않는다.
- companion은 루프백 외 주소에 바인딩하지 않는다.
- 요청 크기, 프로토콜 버전, client ID, 인증값을 검증한다.

## E2E

- Aside와 Edge의 전체 user-data root를 임시 위치에 복제한다.
- 복제 프로필만 `--user-data-dir`, `--profile-directory=Default`, `--load-extension`으로 실행한다.
- 북마크 추가, 수정, 삭제와 잘못된 암호를 실제 브라우저에서 검증한다.
- 실행 전후 원본 `Bookmarks` 체크섬을 비교하고 모든 프로세스, 포트, 임시 런타임을 정리한다.
